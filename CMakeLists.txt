SET(BUILD_VISUALIZATION OFF)
IF(BUILD_VISUALIZATION)
	set(the_description "The UbiTrack Visualization Components")
	configure_file(src/utVisualization/WebGui/config_cmake.h config.h)
	ut_add_module(utvisualization utcore utdataflow utvision ${OPENGL_LIBRARIES} ${Freeglut_LIBRARIES})
	ut_module_include_directories(${TINYXML_INCLUDE_DIR} ${LOG4CPP_INCLUDE_DIR} ${BOOSTBINDINGS_INCLUDE_DIR} ${LAPACK_INCLUDE_DIR}  ${Boost_INCLUDE_DIR} ${OPENGL_INCLUDE_DIR} ${Freeglut_INCLUDE_DIR})
	ut_glob_module_sources(HEADERS "src/Render/*.h" "src/Render/*/*.h" SOURCES "src/Render/*.cpp")
	ut_glob_component_sources(HEADERS "src/OpenCV/*.h" SOURCES "src/OpenCV/*.cpp")
	ut_create_module()
ENDIF(BUILD_VISUALIZATION)
